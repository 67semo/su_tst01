# 확장자가 .a인 파일  무시
*.a

# 위에서 확잗아 .a일 파일을 무시하게 했지만 lib.a는 무시하지 않음.
!lib.a

# 현제 directory에서 todo 파일은 무시하고 하위directory에서는 추적
/todo

# directory /build에 있는 파일 전체 무시
build/

# doc directory의 모든 txt파일 무시하고 그 하위디렉토리의 파일들은 추적
doc/*.txt

# doc directory와 그 하위 directory의 모든 txt화일 무식
doc/**/*.txt